import { createClient } from "@/lib/supabase/server";
import { redirect } from "next/navigation";
export default async function Admin(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const {data:profile}=await supabase.from("profiles").select("role").eq("id",user.id).single();if(profile?.role!=="admin")return <div><h1 className="text-3xl font-black">Access denied</h1><p className="mt-2 text-slate-500">Your account is not an admin.</p></div>;return <div><h1 className="text-4xl font-black">Admin Dashboard</h1><p className="mt-3 text-slate-500">Business control center for Nasla Export.</p><div className="mt-8 grid gap-5 md:grid-cols-4">{["Templates","Domains","Orders","Inquiries"].map(x=><div className="card" key={x}><div className="text-sm text-slate-500">{x}</div><div className="mt-2 text-3xl font-black">—</div></div>)}</div></div>}